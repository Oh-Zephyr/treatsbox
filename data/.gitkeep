This file exists only so git tracks the data/ directory (git doesn't track
empty folders). db.json itself is gitignored on purpose — it's runtime data,
not source. See lib/db.js for the same fix applied defensively at runtime.
